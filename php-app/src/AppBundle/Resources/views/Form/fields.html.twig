{% block media_preview_widget %}
    {% spaceless %}
        {% if path is defined and path is not null %}
            {%  if type == 'image' %}
                <img src="{{ path | imagine_filter('small') }}"/>
            {% elseif type == 'video' %}
                <video width="260" controls>
                    <source src="{{ assets.s3prefix }}{{ path }}" type="video/mp4">
                </video>
            {% else %}
                <a download href="{{ assets.s3prefix }}{{ path }}">
                   {{ path }}
                </a>
            {%  endif %}
        {% endif %}
    {% endspaceless %}
{% endblock %}

{% block order_view_widget %}
    {% spaceless %}
        <div style="width: 100%; overflow-x: auto;">
            {% include '@App/Web/partial/order.html.twig'  with {
                'order': order,
                'showDelivery': true,
                'summary': false,
                'showTaxTable': true,
                'showAddresses': true
            } %}
            <style>
                .table--tax {
                    width: 35%;
                    display: inline-block;
                }
            </style>
        </div>
    {% endspaceless %}
{% endblock %}

{% block incremental_number_widget %}
    {% spaceless %}
    <input type="number"
        value="{{ value }}"
        readonly
        class="form-control" 
        {{ block('widget_attributes') }} 
        style="width: 35%; display: inline-block;"
    />
    <div style="margin-left: 4%; width: 60%; display: inline-block;">
        <label for="amount">{{ 'Entrada'|trans }}:</label>
        <input type="incrementAmount" id="incrementAmount" value="0" style="width: 50px">
        <button id="increment" onclick="event.preventDefault(); this.parentNode.previousSibling.value = +this.parentNode.previousSibling.value + +this.previousSibling.value">
            {{ 'Añadir'|trans }}
        </button>
    </div>
    {% endspaceless %}
{% endblock %}